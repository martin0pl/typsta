# typsta

A little tool to help you create new Typst project with a template.

## Roadmap

### V0

- [ ] data persistence for the templates path in the file ~/.typsta-config.toml
- [ ] command `typsta config`
- [ ] command `typsta config <templates_path>`
- [ ] command `typsta list`
- [ ] command `typsta new <template_name>`
- [ ] command `typsta update`
